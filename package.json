{
  "name": "dropalert",
  "private": true,
  "description": "Vercel: leave Root Directory empty (repo root). Do not set Root Directory to frontend while using root vercel.json (paths would break).",
  "dependencies": {
    "next": "16.2.6",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  }
}
